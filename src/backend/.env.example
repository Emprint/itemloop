APP_ENV=production
APP_DEBUG=false

DB_HOST=localhost
DB_NAME=itemloop
DB_USER=your_db_user
DB_PASS=your_db_password
DB_CHARSET=utf8mb4

# Must match your domain — used for CORS and cookie settings
APP_URL=https://yourdomain.com
