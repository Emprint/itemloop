; PHP-FPM limits for the site — phone photos routinely exceed the 2M default.
; Deployed by deploy.sh; must stay in sync with deploy/frontend-api.user.ini.
upload_max_filesize = 20M
post_max_size = 25M
